# scratch / review exports
*_export.txt
system_export.txt

# unused screenshot source -- inline SVGs are the live direction.
# if screenshots are ever wired in, they must live under docs/ (GitHub Pages serves only /docs).
public/

# OS cruft
.DS_Store
Thumbs.db
